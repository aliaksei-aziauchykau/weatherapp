services:
  ui:
    build:
      context: ./src/services/ui
      dockerfile: .docker/local/Dockerfile
    image: weatherapp-ui:local
    container_name: weatherapp-ui
    environment:
      WEATHER_API_PUBLIC_BASE_URL: ${WEATHER_API_PUBLIC_BASE_URL}
    depends_on:
      - api
    networks:
      - weatherapp-network
    restart: unless-stopped
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.wui.rule=Host(`ui.weatherapp.local.projects.volnyja-siervisy.site`)"
      - "traefik.http.routers.wui.entrypoints=web"
      - "traefik.http.services.wui.loadbalancer.server.port=80"
